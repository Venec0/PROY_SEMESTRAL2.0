import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PROY_SEMESTRAL_APPS_MOV',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
